name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  packages: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux-binaries:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: linux
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-4-dev libadwaita-1-dev libglib2.0-dev
      - name: Build release
        run: cargo build --release
        working-directory: linux
      - name: Package binary tarball
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          TARBALL="pocketlens-${VERSION}-linux-amd64.tar.gz"
          mkdir -p staging/bin staging/share/pocketlens
          cp linux/target/release/pocketlens-receiver staging/bin/
          cp linux/target/release/pocketlens-gtk staging/bin/
          cp android/app/build/outputs/apk/debug/*.apk staging/share/pocketlens/pocketlens.apk 2>/dev/null || true
          tar -czf "${TARBALL}" -C staging .
          echo "TARBALL=${TARBALL}" >> "$GITHUB_ENV"
      - uses: actions/upload-artifact@v4
        with:
          name: linux-tarball
          path: pocketlens-*-linux-amd64.tar.gz

  build-deb:
    runs-on: ubuntu-latest
    needs: build-linux-binaries
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: linux-binaries
          path: bin/
      - uses: actions/download-artifact@v4
        with:
          name: android-apk
          path: apk/ 2>/dev/null || true
      - name: Build .deb
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          PKGDIR="pocketlens_${VERSION}_amd64"
          mkdir -p "${PKGDIR}/DEBIAN"
          mkdir -p "${PKGDIR}/usr/bin"
          mkdir -p "${PKGDIR}/usr/share/applications"
          mkdir -p "${PKGDIR}/usr/share/pocketlens"
          cp bin/pocketlens-receiver "${PKGDIR}/usr/bin/"
          cp bin/pocketlens-gtk "${PKGDIR}/usr/bin/"
          chmod 755 "${PKGDIR}/usr/bin/"*
          cp apk/*.apk "${PKGDIR}/usr/share/pocketlens/pocketlens.apk" 2>/dev/null || true
          cat > "${PKGDIR}/DEBIAN/control" << EOF
          Package: pocketlens
          Version: ${VERSION}
          Section: video
          Priority: optional
          Architecture: amd64
          Depends: libgtk-4-1, gstreamer1.0-plugins-base, gstreamer1.0-plugins-good, gstreamer1.0-libav, v4l2loopback-dkms, pipewire, pulseaudio-utils
          Maintainer: PocketLens <noreply@github.com>
          Description: Use an Android phone as a Linux camera and microphone
          PocketLens streams your Android camera and microphone to a Linux desktop,
          creating virtual video and audio devices for any application.
          EOF
          cat > "${PKGDIR}/usr/share/applications/pocketlens.desktop" << EOF
          [Desktop Entry]
          Type=Application
          Name=PocketLens
          Comment=Use an Android phone as a Linux camera and microphone
          Exec=/usr/bin/pocketlens-gtk
          Terminal=false
          Categories=AudioVideo;Video;
          Icon=camera-web
          EOF
          dpkg-deb --root-owner-group --build "${PKGDIR}"
          echo "DEB=${PKGDIR}.deb" >> "$GITHUB_ENV"
      - uses: actions/upload-artifact@v4
        with:
          name: deb-package
          path: pocketlens_*.deb

  build-appimage:
    runs-on: ubuntu-latest
    needs: build-linux-binaries
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: linux-binaries
          path: bin/
      - uses: actions/download-artifact@v4
        with:
          name: android-apk
          path: apk/ 2>/dev/null || true
      - name: Build AppImage
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          APPDIR="PocketLens.AppDir"
          mkdir -p "${APPDIR}/usr/bin" "${APPDIR}/usr/share/applications" "${APPDIR}/usr/share/pocketlens" "${APPDIR}/usr/share/icons/hicolor/256x256/apps"
          cp bin/pocketlens-receiver "${APPDIR}/usr/bin/"
          cp bin/pocketlens-gtk "${APPDIR}/usr/bin/"
          chmod 755 "${APPDIR}/usr/bin/"*
          cp apk/*.apk "${APPDIR}/usr/share/pocketlens/pocketlens.apk" 2>/dev/null || true
          cat > "${APPDIR}/pocketlens.desktop" << EOF
          [Desktop Entry]
          Type=Application
          Name=PocketLens
          Comment=Use an Android phone as a Linux camera and microphone
          Exec=pocketlens-gtk
          Terminal=false
          Categories=AudioVideo;Video;
          Icon=camera-web
          EOF
          cat > "${APPDIR}/AppRun" << 'APPRUN'
          #!/bin/bash
          APPDIR="$(dirname "$(readlink -f "$0")")"
          export PATH="${APPDIR}/usr/bin:${PATH}"
          exec pocketlens-gtk "$@"
          APPRUN
          chmod +x "${APPDIR}/AppRun"
          VERSION="${VERSION}" appimage/appimagetool --appimage-extract-and-run "${APPDIR}" "PocketLens-${VERSION}-x86_64.AppImage" 2>/dev/null || \
            echo "appimagetool not available, skipping AppImage build"
      - uses: actions/upload-artifact@v4
        with:
          name: appimage
          path: PocketLens-*.AppImage

  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v6
      - name: Build APK
        run: ./gradlew assembleDebug
        working-directory: android
      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: android/app/build/outputs/apk/debug/*.apk

  publish:
    runs-on: ubuntu-latest
    needs: [build-linux-binaries, build-deb, build-appimage, build-apk]
    steps:
      - uses: actions/download-artifact@v4
      - name: Collect artifacts
        run: |
          mkdir -p release
          find . -name "*.deb" -exec cp {} release/ \;
          find . -name "*.AppImage" -exec cp {} release/ \;
          find . -name "*.tar.gz" -exec cp {} release/ \;
          find . -name "*.apk" -exec cp {} release/ \;
          ls -la release/
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
          draft: false
          prerelease: false

  publish-deb-repo:
    runs-on: ubuntu-latest
    needs: build-deb
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: deb-package
      - name: Publish to GitHub Packages APT repo
        run: |
          echo "Publishing .deb to GitHub Packages APT repository"
          find . -name "*.deb" -ls
          # GitHub Packages APT repo publish via gh CLI
          # gh release upload ... --clobber
